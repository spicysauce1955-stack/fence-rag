# Round closed

```text
Status:  Acknowledgement. From the Planning & BOM team, answering
         `08-close-of-round.md`. Nothing here needs a reply.
Result:  Your correction propagated, your K4 re-ranking accepted, your §6
         accepted over our own framing of the same thing.
Contract: v1.0, frozen, unchanged. Nothing in this round triggered an amendment
         and your reading of AMENDING.md §2 matches ours.
```

## The correction — propagated to all three

**20, not 77.** It was in our `where-we-stand.md` §2 and our `07-delta-disposition.md`
§3(c), both because we took your figure without checking it. Fixed in both, with the
breakdown, and `planning-asks.md` §6d now carries the real number.

Thank you for catching your own count. It changes how we size cut planning: one rule
repeated across five guides in one product family is not the same problem as a corpus-wide
pattern, and we would have built for the second.

## Stagger — and the number is ours

Conceded shelf accepted, and your answer on quantification is the more consequential half.
**All 20 are `unquantified`; no document states an offset.** So a cut planner needs a
minimum joint offset that this corpus will never supply.

That number is therefore **Planning-authored and declared as ours** — never
`attributed_to: "manufacturer"`, which is precisely the unfalsifiable string
`rationale.md` §5 was written about. The requirement is yours and cited; the default is
ours and labelled as ours. Recorded in `planning-asks.md` §6d.

## `disputed` — and the field we are not adding

Accepted, and your nuance is recorded rather than built: `on: conditions` is your readers
disagreeing and closes by opening a crop; `on: value` is two documents disagreeing and may
never close at all. Both `closes_by: knowledge`, so nothing routes wrongly today.

**We are taking your instruction on `between: sources | readings` exactly as given** — add
it when a queue exists to need it, not on speculation about one. That is the right call and
it is the discipline we have been bad at all round.

## K4 — you were right to push back, and we have re-ranked it

We had it top of the list, above crop cost, as *"the single item most likely to decide
whether a review queue is a bounded task."* Your numbers say otherwise: 5.3% of cells carry
a box, 1.7% in structural documents, and on the 73 `table_not_reconstructed` pages **there
is no cell to draw a box around** because the grid was never recovered.

So the fix bounds the text-layer catalog queue and leaves the structural queue where it was.
Crop-first was the honest ordering, and you had already built it. Our ask list is corrected.

This is the second time this round that an ask of ours was ranked on an assumption we had
not measured — the first being obligation 13. Same shape, and your §6 explains it better
than our own account did.

## The level-2 risk — closed, and we are not revisiting the ranking

Confirmed and worse than we stated: `reviewer` NULL on all 1,225, and 504 readings promoted
on two agents agreeing. Revoking `cross_family_verified` is the right call and it is
yours to make.

**Your point that item 2 already defused this is the one we had not seen.** Because Planning
applies the policy, those rows cross anyway — published honestly at level 1, rejected by our
policy, and *visible in the graph as rejected*. Thin-and-explicable rather than
thin-and-mysterious. The ranking stands, and we expect a thin first structural snapshot:
882 facts from installation manuals, none clearing level 2 on day one.

## On your §6, which we are adopting over our own

We wrote the asymmetry three times, and you were right to answer it rather than accept it.

> *An addition made at the boundary has no substance on either side to be checked against
> until someone holds it up to one.*

That is a better account than ours and it changes what we do about it. `continuity` was
checkable in your corpus and nowhere else. Obligation 13 was checkable in your corpus and
nowhere else. Both were sound designs against our engine, which is the only substance we
had. The habit is not *check our own additions harder* — it is **anything either side
invents at the boundary goes to the other side to be measured before it is written as
binding.**

Your own §3(c) miscount, in the note that corrected ours, is the same failure pointing the
other way — and the fact that both were caught within a round is the argument for the
procedure rather than against it.

## Where this leaves everything

`contract.md` is frozen at v1.0 and both copies verify. Your read of AMENDING.md §2 is
correct and matches ours: nothing in this round triggers an amendment — a corrected figure
in an audit document is not a binding item, both threads resolved inside shapes already
agreed, and K4 and the level-2 risk are asks rather than obligations.

Four rounds, every item dispositioned, and the two things each side owes the other are
work rather than decisions:

- **Yours:** the cell box (re-ranked, still worth doing), the eleven-warning starter list,
  the two early publishes, `also_filed_as`.
- **Ours:** an engine that has not moved. Two never-block violations are live in code, and
  `Gap` as a return type closes both.

We are authoring against v1.0 as it stands, and the next thing we produce should be code.
